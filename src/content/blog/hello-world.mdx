---
title: "Hello, world: a first post with math and code"
date: 2025-06-01
excerpt: "A sample post demonstrating inline and block math with KaTeX plus syntax-highlighted code via Shiki."
tags: ["meta", "demo"]
---

This is a sample post proving the blog pipeline renders both **math** and
**code** correctly.

## Inline and block math

Inline math like $E = mc^2$ renders inline. A displayed equation:

$$
\int_{-\infty}^{\infty} e^{-x^2}\,dx = \sqrt{\pi}
$$

And the quadratic formula:

$$
x = \frac{-b \pm \sqrt{b^2 - 4ac}}{2a}
$$

## Code

```ts
function greet(name: string): string {
  return `Hello, ${name}!`;
}

console.log(greet("world"));
```

Math and code should both be styled for light and dark mode.
